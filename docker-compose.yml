version: "3.7"
services:
  pulsar:
    image: apachepulsar/pulsar-all:2.5.0
    environment:
      - PULSAR_MEM=" -Xms512m -Xmx512m -XX:MaxDirectMemorySize=1g"
    command: >
      /bin/bash -c
      "bin/apply-config-from-env.py conf/standalone.conf
      && bin/pulsar standalone"
    volumes:
      - "/pulsar/data"
      - "/pulsar/conf"
      - "./engine/workflow-engine/build/libs:/engine/workflow-engine/build:delegated"
    ports:
      - "6650:6650"
      - "8080:8080"
