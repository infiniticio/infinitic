package io.infinitic.workflowManager.worker

import io.infinitic.workflowManager.common.data.branches.Branch
import io.infinitic.workflowManager.common.data.commands.Command

class BranchHandler(workflow: Any, branch: Branch) {

    fun handle(commands: MutableList<Command>) {
    }
}
